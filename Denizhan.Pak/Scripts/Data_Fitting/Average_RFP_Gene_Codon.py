import sys
import csv
import numpy

totalRFP = 0

class Gene:
    def __init__(self, name):
        self.id = name
        self.codons = []

class Codon:
    def __init__(self, numpos, cod, gen, rfp):
        self.codons = cod
        self.rfps = numpy.array(rfpcounts, dtype=float)
        self.count

with
